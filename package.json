{
  "name": "iport",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 4455",
    "build": "rm -rf ./out && next build && next export && echo iport.hejny.org > ./out/CNAME && echo > ./out/.nojekyll",
    "start": "next start",
    "lint": "next lint",
    "test": "echo \"No tests yet\"",
    "prettier": "prettier --config .prettierrc --ignore-unknown --write \"{pages,public,styles}/**/*\"",
    "prettier-commit": "git diff --exit-code && npm run prettier && git add . && git commit --allow-empty -m \"🧹 Prettier\"",
    "preversion": "git pull && npm run prettier-commit&& npm run lint && npm test",
    "postversion": "git push && git push --tags && echo \"Check the deploy process: https://github.com/hejny/iport/actions/\""
},
  "dependencies": {
    "@next/font": "13.1.4",
    "@types/node": "18.11.18",
    "@types/react": "18.0.27",
    "@types/react-dom": "18.0.10",
    "eslint": "8.32.0",
    "eslint-config-next": "13.1.4",
    "next": "13.1.4",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "typescript": "4.9.4"
  }
}
